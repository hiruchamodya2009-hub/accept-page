<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Accept?</title>

  <style>
    body {
      font-family: Arial, sans-serif;
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      overflow: hidden;
    }

    h2 {
      margin-bottom: 20px;
    }

    #area {
      position: relative;
      width: 300px;
      height: 150px;
    }

    button {
      position: absolute;
      padding: 10px 20px;
      font-size: 16px;
      cursor: pointer;
    }

    #yes {
      left: 40px;
      top: 50px;
    }

    #no {
      right: 40px;
      top: 50px;
    }

    #slipper {
      position: absolute;
      top: 50%;
      left: -200px;
      width: 120px;
      display: none;
      animation: throwSlipper 1s ease-out forwards;
    }

    @keyframes throwSlipper {
      0% { transform: translateX(0) rotate(0deg); }
      100% { transform: translateX(1200px) rotate(720deg); }
    }
  </style>
</head>

<body>

  <h2>Do you accept?</h2>

  <div id="area">
    <button id="yes">Yes</button>
    <button id="no">No</button>
  </div>

  <img id="slipper" src="slipper.png" alt="slipper">

  <script>
    const noBtn = document.getElementById("no");
    const yesBtn = document.getElementById("yes");
    const slipper = document.getElementById("slipper");

    noBtn.addEventListener("mouseover", () => {
      const x = Math.random() * 220;
      const y = Math.random() * 100;
      noBtn.style.left = x + "px";
      noBtn.style.top = y + "px";
    });

    yesBtn.addEventListener("click", () => {
      slipper.style.display = "block";
    });
  </script>

</body>
</html>
